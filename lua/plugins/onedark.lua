return {
  "joshdick/onedark.vim",
  lazy = false,
  priority = 1000,

  config = function()
    -- require("xcode").setup({
    --   options = {},
    -- })
    --
    -- vim.cmd("colorscheme onedark")
  end,
}
